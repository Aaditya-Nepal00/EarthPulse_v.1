[phases.setup]
nixPkgs = ["python311", "gcc", "gdal", "proj", "geos"]

[phases.install]
cmds = ["cd backend", "pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "cd backend && python -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
